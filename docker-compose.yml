services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: hippo
      POSTGRES_PASSWORD: hippo
      POSTGRES_DB: hippo
    ports:
      - "5433:5432"
    volumes:
      - hippo_pg:/var/lib/postgresql/data
volumes:
  hippo_pg:
