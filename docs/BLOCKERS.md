# Blockers (inputs only the human can provide)

Format: date — what — why — where it goes. Remove a line once resolved.

- 2026-09-22 — Operator `MEMWAL_ACCOUNT_ID` + `MEMWAL_PRIVATE_KEY` (mainnet, created on memory.walrus.xyz with the Sessions wallet) — needed for `pnpm smoke --write`, `pnpm hippo`, and every M1 spike — `.env` (currently placeholders).
- 2026-09-22 — `OPENROUTER_API_KEY` with a few dollars of credit — needed for any chat turn — `.env`.
- 2026-09-22 — `TELEGRAM_BOT_TOKEN` from @BotFather — needed for M2 Telegram verification — `.env`.
